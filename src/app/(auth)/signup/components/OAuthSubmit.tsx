const OAuthSubmit = () => {
  const currentParams = new URLSearchParams(window.location.search);
  if (currentParams.get("isOAuth") === "true") {
    try {
      
    } catch (error) {
      
    }
  }
};
